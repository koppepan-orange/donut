{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "name": "from now on(html)",
            "request": "launch",
            "file": "${file}",
        },
    ]
}